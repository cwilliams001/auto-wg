# Fail2ban filter for port scans
# Generated by Auto-WG Ansible

[INCLUDES]
before = common.conf

[Definition]
failregex = ^.*kernel:.*\[UFW BLOCK\].*SRC=<HOST>.*DPT=\d+.*$
            ^.*kernel:.*DROP.*SRC=<HOST>.*$

ignoreregex =

# Multiple failures within a short time indicate scanning
maxlines = 1